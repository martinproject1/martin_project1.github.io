alert('div text'); {
    <i class="fas fa-vihara    tashchyanstyle"></i>
}
CSSLayerStatementRule(
    KeyboardEvent(' ' )
)

for (const key in object) {
    if (Object.hasOwnProperty.call(object, key)) {
        const element = object[key];
        alert(div)
    }
}
